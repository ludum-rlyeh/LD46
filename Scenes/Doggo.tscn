[gd_scene load_steps=4 format=2]

[ext_resource path="res://icon.png" type="Texture" id=1]

[sub_resource type="GDScript" id=2]
script/source = "extends KinematicBody2D

var speed = 2.0

# Called when the node enters the scene tree for the first time.
func _ready():
	set_process_input(true)

func _input(event):
	if event.is_action(\"ui_right\") or event.is_action(\"ui_left\")\\
		or event.is_action(\"ui_down\") or event.is_action(\"ui_up\"):
		
		var direction = Vector2(0.0,0.0)
		
		if event.is_action(\"ui_right\"):
			direction.x += 1.0
		elif event.is_action(\"ui_left\"):
			direction.x -= 1.0
		if event.is_action(\"ui_down\"):
			direction.y += 1.0
		elif event.is_action(\"ui_up\"):
			direction.y -= 1.0
		
		self.move_and_collide(speed * direction)


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
"

[sub_resource type="RectangleShape2D" id=1]
extents = Vector2( 32.3824, 33.0942 )

[node name="Doggo" type="KinematicBody2D"]
script = SubResource( 2 )

[node name="Sprite" type="Sprite" parent="."]
modulate = Color( 1, 0.764706, 0.317647, 1 )
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource( 1 )
